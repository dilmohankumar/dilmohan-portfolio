NODE_ENV=development
PORT=5000

MONGODB_URI=mmongodb+srv://dilmohankumar:yn7j2RQS4MwA4GGv@portfolio.0axmi.mongodb.net/?appName=portfolio

JWT_SECRET=replace-with-a-long-random-string
JWT_EXPIRES_IN=7d

CLIENT_ORIGIN=http://localhost:5173

# Credentials for the ONE-TIME seed script (server/src/seed/seed.js) that creates the admin account.
# Not read anywhere else. Change the password after first login is not supported yet — re-run seed
# against an empty Admin collection if you ever need to rotate it.
ADMIN_EMAIL=kdilmohan101@gmail.com
ADMIN_PASSWORD=Dkr@101101

BCRYPT_SALT_ROUNDS=12

LOGIN_RATE_LIMIT_WINDOW_MS=900000
LOGIN_RATE_LIMIT_MAX=8
